/** Exam ops layout — workflow tool, not KPI hub. */
export const EXAMS_PAGE_CLASS = 'max-w-[1200px] px-4 py-5 sm:px-6 lg:px-8 lg:py-6';

export const EXAMS_UI = {
  navItem:
    'flex w-full min-h-[44px] items-center gap-2.5 rounded-xl px-3 py-2.5 text-left text-[13px] font-semibold transition-colors',
  navItemActive: 'bg-brand-50 text-brand-900 ring-1 ring-brand-200/60',
  navItemInactive: 'text-neutral-600 hover:bg-neutral-50 hover:text-neutral-900',
} as const;
